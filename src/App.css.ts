import { css } from "@emotion/react";

const cssStyles = {
    app: css({
        margin: '0 auto',
        display: 'flex',
        flexDirection: 'column',
        paddingTop: 152,
    }),
};

export default cssStyles;