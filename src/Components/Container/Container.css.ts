import { css } from '@emotion/react';

const cssStyles = {
	container: css({
		maxWidth: 1170,
		width: '100%',
		margin: '0 auto',
		display: 'flex',
		flexDirection: 'column',
		paddingRight: 24,
		paddingLeft: 24,
	}),
};

export default cssStyles;
